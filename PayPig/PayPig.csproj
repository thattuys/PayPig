<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Dalamud.NET.Sdk/15.0.0">
  <PropertyGroup>
    <Authors>You</Authors>
    <Company></Company>
    <Version>0.5.0.0</Version>
    <Description>A Dalamud plugin.</Description>
    <PackageProjectUrl></PackageProjectUrl>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <!--
    Dalamud.NET.Sdk pulls in the Dalamud reference assemblies automatically from
    %AppData%\XIVLauncher\addon\Hooks\dev (override with the DALAMUD_HOME env var),
    targets net9.0-windows, and handles DalamudPackager output. Add NuGet
    PackageReferences below as needed (e.g. ECommons, plugin API packages).
  -->
  <ItemGroup>
    <!-- <PackageReference Include="DalamudPackager" Version="11.0.0" /> -->
  </ItemGroup>
</Project>
